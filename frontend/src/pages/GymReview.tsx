import { useEffect } from "react"


const GymReview = () => {

    // useEffect( () => {
    //     fetch("http://localhost:8080/users").then(r => r.json()).then(r => console.log(r)).catch(e => console.log(e));
        
    // },[])

    // const loadUsers = () => {
    //     fetch("http://localhost:8080/users").then(r => r.json()).then(r => console.log(r)).catch(e => console.log(e));
    // }
  return (
    <div>GymReview</div>
  )
}

export default GymReview